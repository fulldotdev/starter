---
import Heading from './Heading.astro'

const {
  structure = 'column',
  size = 'lg',
  variant = 'subtle',
  align = 'center',
  level = 2,
  badge,
  tagline,
  heading,
  text,
  buttons,
  frame = 'panel',
  input,
  ...rest
} = Astro.props
---

<section {...rest}>
  <div>
    <Heading>{heading}</Heading>
    <Heading text={heading} />
    <Heading html={heading} />
  </div>
</section>
