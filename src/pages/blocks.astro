---
import Layout from '@layouts/Layout.astro'
import { getEntry } from 'astro:content'
import { Blocks as FullBlocks } from 'fulldev-ui'

const page = await getEntry('pages', 'blocks')

const { blocks, hero, content, features } = page.data

console.log(blocks)
---

<Layout>
  <FullBlocks {...blocks} />
</Layout>
