---
import Blocks from '@components/Blocks.astro'
import Layout from '@layouts/Layout.astro'

import { getEntry } from 'astro:content'

const page = await getEntry('pages', 'index')

const { blocks, hero } = page.data
---

<Layout>
  <Blocks {blocks} />
</Layout>
