---
import Missing from '@blocks/Missing.astro'
import Layout from '@layouts/Layout.astro'
import { getEntry } from 'astro:content'

const page = await getEntry('pages', '404')

const { content } = page.data
---

<Layout>
  <Missing {...content} />
</Layout>
